import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#285C60',
    },
    secondary: {
      main: '#F3F1ED',
    },
  },
  typography: {
    fontFamily: 'Poppins, sans-serif',
  },
})
